
const dict={fr:{inventory:"Inventaire",financing:"Financement",about:"À propos",contact:"Contact",heroTitle:"Trouvez votre prochain véhicule chez Sharbo Auto",heroText:"Véhicules d'occasion de qualité, financement flexible et service professionnel au Québec.",viewInventory:"Voir l'inventaire",apply:"Demande de financement",why:"Pourquoi choisir Sharbo Auto",featured:"Véhicules en vedette",details:"Voir les détails"},en:{inventory:"Inventory",financing:"Financing",about:"About",contact:"Contact",heroTitle:"Find your next vehicle at Sharbo Auto",heroText:"Quality pre-owned vehicles, flexible financing and professional service in Quebec.",viewInventory:"View inventory",apply:"Apply for financing",why:"Why choose Sharbo Auto",featured:"Featured vehicles",details:"View details"}};
let lang=localStorage.getItem("lang")||"fr";
function t(){document.querySelectorAll("[data-t]").forEach(e=>{e.textContent=dict[lang][e.dataset.t]||e.textContent});document.documentElement.lang=lang;}
function toggleLang(){lang=lang==="fr"?"en":"fr";localStorage.setItem("lang",lang);t();}
async function getCars(){try{const data=await fetch("data/vehicles.json",{cache:"no-store"}).then(r=>r.json());return data.vehicles||[]}catch(e){console.error(e);return []}}
function carCard(c){const img=(c.images&&c.images[0])||"assets/images/logo.jpeg";return `<article class="car"><img src="${img}" alt="${c.title}"><div class="car-body"><h3>${c.title}</h3><div class="price">$${Number(c.price||0).toLocaleString("fr-CA")}</div><div class="specs"><span>${c.year||""}</span><span>${Number(c.mileage||0).toLocaleString("fr-CA")} km</span><span>${c.transmission||""}</span><span>${c.fuel||""}</span></div><a class="btn" href="vehicle.html?id=${encodeURIComponent(c.id)}" data-t="details">Voir les détails</a></div></article>`;}
async function loadFeatured(){const el=document.getElementById("featuredCars");if(!el)return;const cars=await getCars();const list=cars.filter(c=>c.featured!==false).slice(0,6);el.innerHTML=list.length?list.map(carCard).join(""):"<p>Aucun véhicule pour le moment.</p>";t();}
async function setupInventory(){const grid=document.getElementById("inventoryGrid");if(!grid)return;const all=await getCars();const q=document.getElementById("q"),brand=document.getElementById("brand"),year=document.getElementById("year"),max=document.getElementById("maxPrice");brand.innerHTML='<option value="">Marque</option>'+[...new Set(all.map(c=>c.brand).filter(Boolean))].sort().map(x=>`<option>${x}</option>`).join("");year.innerHTML='<option value="">Année</option>'+[...new Set(all.map(c=>c.year).filter(Boolean))].sort((a,b)=>b-a).map(x=>`<option>${x}</option>`).join("");function render(){const query=(q.value||"").toLowerCase();const list=all.filter(c=>(!query||`${c.title} ${c.brand} ${c.model} ${c.year}`.toLowerCase().includes(query))&&(!brand.value||c.brand===brand.value)&&(!year.value||String(c.year)===String(year.value))&&(!max.value||Number(c.price)<=Number(max.value)));grid.innerHTML=list.length?list.map(carCard).join(""):"<p>Aucun véhicule trouvé.</p>";t();}document.querySelectorAll(".filter").forEach(e=>e.addEventListener("input",render));render();}
async function setupVehicle(){const el=document.getElementById("vehicleDetail");if(!el)return;const id=new URLSearchParams(location.search).get("id");const cars=await getCars();const c=cars.find(x=>String(x.id)===String(id))||cars[0];if(!c){el.innerHTML="<p>Véhicule introuvable.</p>";return;}const img=(c.images&&c.images[0])||"assets/images/logo.jpeg";const carfax=c.carfax?`<a class="btn outline" href="${c.carfax}" target="_blank">Voir Carfax</a>`:"";el.innerHTML=`<div class="about"><div><img class="detail-img" src="${img}" alt="${c.title}"></div><div><h1>${c.title}</h1><div class="price">$${Number(c.price||0).toLocaleString("fr-CA")}</div><p>${c.description_fr||"Rapport Carfax disponible sur demande. Contactez-nous pour plus d'information."}</p><p><b>VIN:</b> ${c.vin||"Sur demande"}</p><p><b>Kilométrage:</b> ${Number(c.mileage||0).toLocaleString("fr-CA")} km</p><p><b>Transmission:</b> ${c.transmission||""}</p><p><b>Carburant:</b> ${c.fuel||""}</p><p><b>Statut:</b> ${c.status||"Disponible"}</p><a class="btn" href="financement.html">Demande de financement</a> <a class="btn dark" href="contact.html">Contact</a> ${carfax}</div></div>`;}
document.addEventListener("DOMContentLoaded",()=>{t();loadFeatured();setupInventory();setupVehicle();});
